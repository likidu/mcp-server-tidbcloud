# TiDB Cloud Authentication Configuration
# OAuth is the default and recommended authentication method.
# API Keys (Digest auth) are supported as a fallback.

# ============================================================
# Environment Configuration
# ============================================================
# Set to "dev" for development/testing or "prod" for production.
# Defaults to "prod" if not specified.
#
# Environment determines which TiDB Cloud endpoints are used:
#   dev:
#     - OAuth: https://dev.tidbcloud.com/oauth/authorize
#     - Token: https://oauth.dev.tidbcloud.com/v1/token
#     - API: https://serverless.dev.tidbapi.com
#   prod:
#     - OAuth: https://tidbcloud.com/oauth/authorize
#     - Token: https://oauth.tidbcloud.com/v1/token
#     - API: https://serverless.tidbapi.com
#
TIDB_CLOUD_ENV=dev

# ============================================================
# Option 1: OAuth Authentication (Default, Recommended)
# ============================================================
# OAuth credentials for TiDB Cloud. When configured, OAuth takes priority.
# Scope: org:owner
#
# To authenticate:
#   1. Visit https://your-server/oauth/login
#   2. Authorize the app in TiDB Cloud
#   3. The access token will be displayed (or set TIDB_CLOUD_OAUTH_ACCESS_TOKEN)
#
TIDB_CLOUD_OAUTH_CLIENT_ID=your-oauth-client-id
TIDB_CLOUD_OAUTH_CLIENT_SECRET=your-oauth-client-secret

# Optional: Pre-obtained access token (skip OAuth flow if you have one)
# TIDB_CLOUD_OAUTH_ACCESS_TOKEN=ticloud_oa_xxxxx

# Optional: Redirect URI for OAuth callback (auto-detected in remote server)
# TIDB_CLOUD_OAUTH_REDIRECT_URI=https://your-server/oauth/callback

# ============================================================
# Option 2: API Key Authentication (Fallback)
# ============================================================
# Get your API keys from TiDB Cloud console: Organization Settings > API Keys
# Used only if OAuth credentials are not provided.
# TIDB_CLOUD_PUBLIC_KEY=your-public-key-here
# TIDB_CLOUD_PRIVATE_KEY=your-private-key-here

# ============================================================
# API Base URL (Optional)
# ============================================================
# Override the default API base URL (determined by TIDB_CLOUD_ENV).
# Only use this if you need to point to a custom endpoint.
# TIDB_CLOUD_API_URL=https://serverless.tidbapi.com

# ============================================================
# Database Connection Configuration (Optional)
# ============================================================
# Required for SQL execution tools (show_databases, db_query, db_execute, etc.)
# Get connection details from your TiDB Cloud cluster's "Connect" dialog
# TIDB_CLOUD_DB_HOST=gateway01.us-east-1.prod.aws.tidbcloud.com
# TIDB_CLOUD_DB_USER=xxxxx.root
# TIDB_CLOUD_DB_PASSWORD=your-database-password
# TIDB_CLOUD_DB_NAME=test
